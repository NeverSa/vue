{
  "name": "angular-admin",
  "version": "1.0.0",
  "description": "a angular admin",
  "main": "index",
  "scripts": {
    "build": "rimraf dist && webpack",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "webpack-dev-server --port 8081 --hot --inline"
  },
  "author": "neversa",
  "license": "ISC",
  "dependencies": {
    "angular": "^1.5.0",
    "angular-ui-router": "^0.3.1"
  },
  "devDependencies": {
    "html-webpack-plugin": "^2.28.0",
    "rimraf": "^2.6.1",
    "webpack": "^2.6.1",
    "webpack-dev-server": "^2.4.5"
  }
}
